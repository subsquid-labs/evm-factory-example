manifestVersion: subsquid.io/v0.1
name: pancakeswap-swaps
version: 1
description: |-
  An example of processing contracts created with factory
build:

deploy:
  secrets:
    - S3_BUCKET_NAME
    - S3_ACCESS_KEY_ID
    - S3_SECRET_ACCESS_KEY
  processor:
    cmd: [ "node", "lib/processor" ]
  api:
    cmd: [ "sleep", "3600" ]
